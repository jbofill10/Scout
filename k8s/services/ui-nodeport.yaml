# DEPRECATED: This NodePort service is no longer needed.
# The application now uses Ingress for unified routing.
# To access the UI, use the Ingress endpoint instead.
#
# See k8s/ingress/ingress.yaml and k8s/setup-ingress.sh for the new setup.
#
# apiVersion: v1
# kind: Service
# metadata:
#   name: ui-nodeport
#   namespace: default
#   labels:
#     app: ui
# spec:
#   type: NodePort
#   selector:
#     app: ui
#   ports:
#     - protocol: TCP
#       port: 80
#       targetPort: 80
#       nodePort: 30080
