node_modules
CLAUDE.md
MATCHING_SOLUTION.md
DEPLOYMENT.md
OBSERVABILITY.md
.claude
.github
bruno*
sample_*.json
*.mcp*

# Go workspace files (local developer preference)
go.work
go.work.sum

# Go vendor directory (regenerate with: go work vendor)
vendor/

# Secrets and sensitive configs
**/*.toml
*.env
.env.*

# Local development configs
config.toml
api.toml

# Systemd service files (may contain local IPs)
*.service

nginx.conf
docker-compose.yml
nginx/nginx.conf